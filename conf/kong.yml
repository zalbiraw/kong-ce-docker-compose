 _format_version: "1.1"
 _transform: true

 services:
 - host: httpbin.org
   name: httpbin
   port: 80
   protocol: http
   routes:
   - name: httpbin
     paths:
     - /httpbin
     strip_path: true